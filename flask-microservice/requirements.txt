# Web Framework
Flask==2.3.3
Flask-CORS==4.0.0
gunicorn==21.2.0

# Environment & Utilities
python-dotenv==1.0.0
python-multipart==0.0.6
requests==2.31.0

# Data Processing (versions compatible with Python 3.12)
numpy
pandas
scipy
scikit-learn

# Computer Vision (headless - no GUI required)
opencv-python-headless
mediapipe
Pillow

# Data Validation
pydantic==1.10.13
typing-extensions>=4.5.0

# Note: tensorflow==2.13.0 is not compatible with Python 3.12.
# The app gracefully handles its absence via try/except imports.
